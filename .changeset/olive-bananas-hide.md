---
"hana-img-viewer-react": major
---

Remove the custom trigger interface. Function `children` no longer render a custom trigger; the thumbnail image is always rendered with click, Enter, and Space handling.
