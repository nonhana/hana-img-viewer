---
'hana-img-viewer-react': major
---

Render the thumbnail as a native `img`. `className`, `style`, and every unknown prop now spread onto the rendered `img` instead of a wrapper element, and built-in sizing styles were removed so the thumbnail sizes like a plain image.